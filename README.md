Implementation of the state space equations for a spring + mass damper system.

This projects uses [`uv`](https://github.com/astral-sh/uv) for easier dependency management, you may run it via `uv run main.py --tbd`.